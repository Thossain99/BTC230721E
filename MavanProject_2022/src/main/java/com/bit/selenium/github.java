package com.bit.selenium;

import java.util.concurrent.TimeUnit;

public class github {
	int a = 23;
	
	
	public void thiscode()
	{
		int d = 34;
	}
	// inexplicit wait is for the whole ui for a given time 
	//explicit wait is for a basic logic 
	
	public void waitForElement(WebElement ele) {
		WebDriverWait wd= new WebDriverWait(dr, 10);
		wd.until(ExpectedConditions.visibilityof(ele);
		return
	}
}
